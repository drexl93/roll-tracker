{{!-- MEAN --}}
<form>
    <div>
        {{!-- MEAN --}}
        <h1 class="roll-tracker-comparison-card-heading">{{localize "ROLL-TRACKER.comparison-card.title"}}</h1>
        <h3 class="roll-tracker-comparison-card-stat-heading">{{localize "ROLL-TRACKER.comparison-card.headers.mean"}}</h3>
        <ul class="roll-tracker-comparison-card-ul">
            <li class="roll-tracker-comparison-card-li">
                <span class="roll-tracker-comparison-card-goodstat">{{localize "ROLL-TRACKER.comparison-card.descriptors.highest"}}: </span>
                {{#if (isOne mean.highest.length)}} {{!-- If the highest mean is held by just one person --}}
                    <span class="roll-tracker-comparison-card-strong">{{mean.highest.[0].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{mean.highest.[0].value}}.
                {{/if}}
                {{#if (isTwo mean.highest.length)}} {{!-- If the highest mean is held by exactly two people --}}
                    <span class="roll-tracker-comparison-card-strong">{{mean.highest.[0].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.and"}} <span class="roll-tracker-comparison-card-strong">{{mean.highest.[1].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{mean.highest.[0].value}}.
                {{/if}}
                {{#if (isThreePlus mean.highest.length)}} {{!-- If the highest mean is held by three or more people --}}
                    {{#each mean.highest}} 
                    {{!-- mean.highest is an array that contains all users who share the highest mean. 
                    We iterate over them to construct the string displayed in the Comparison Chat Card --}}
                        <span class="roll-tracker-comparison-card-strong">{{this.name}}</span> {{!-- We begin by adding the name to the string --}}
                        {{~#if (isSecondLast @index ../mean.highest.length)~}} 
                        {{!-- @index refers to the current item in the array being iterated over. We must use ../ to access properties of the parent
                        array while within the iteration. Here we're checking to see if the current item is the second last in the array. If so, we
                        add the word 'and' to the string.
                        
                        The squiggly lines just remove whitespace at that end of the expression--}}
                            , {{localize "ROLL-TRACKER.comparison-card.misc.and"}}
                        {{~else~}}
                            {{#if (isLast @index ../mean.highest.length)}}
                                {{!-- If this is the last item in the array, end the string with "with (highest mean)" and a full stop. --}}
                                {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{this.value}}.
                            {{~else~}}
                                , {{!-- If this is isn't the last item in the array, add a comma and keep going --}}
                            {{/if}}
                        {{/if}}
                    {{/each}}
                {{/if}}
            </li>
            {{!-- As above, so below --}}
            <li class="roll-tracker-comparison-card-li">
                <span class="roll-tracker-comparison-card-badstat">{{localize "ROLL-TRACKER.comparison-card.descriptors.lowest"}}: </span>
                {{#if (isOne mean.lowest.length)}}
                    <span class="roll-tracker-comparison-card-strong">{{mean.lowest.[0].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{mean.lowest.[0].value}}.
                {{/if}}
                {{#if (isTwo mean.lowest.length)}}
                    <span class="roll-tracker-comparison-card-strong">{{mean.lowest.[0].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.and"}} <span class="roll-tracker-comparison-card-strong">{{mean.lowest.[1].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{mean.lowest.[0].value}}.
                {{/if}}
                {{#if (isThreePlus mean.lowest.length)}}
                    {{#each mean.lowest}}
                        <span class="roll-tracker-comparison-card-strong">{{this.name}}</span>
                        {{~#if (isSecondLast @index ../mean.lowest.length)~}}
                            , {{localize "ROLL-TRACKER.comparison-card.misc.and"}}
                        {{~else~}}
                            {{#if (isLast @index ../mean.lowest.length)}}
                                {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{this.value}}.
                            {{~else~}}
                                ,
                            {{/if}}
                        {{/if}}
                    {{/each}}
                {{/if}}
            </li>
        </ul>

{{!--
* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
--}}

{{!-- MEDIAN --}}
        <h3 class="roll-tracker-comparison-card-stat-heading">{{localize "ROLL-TRACKER.comparison-card.headers.median"}}</h3>
        <ul class="roll-tracker-comparison-card-ul">
            <li class="roll-tracker-comparison-card-li">
                <span class="roll-tracker-comparison-card-goodstat">{{localize "ROLL-TRACKER.comparison-card.descriptors.highest"}}: </span>
                {{#if (isOne median.highest.length)}}
                    <span class="roll-tracker-comparison-card-strong">{{median.highest.[0].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{median.highest.[0].value}}.
                {{/if}}
                {{#if (isTwo median.highest.length)}}
                    <span class="roll-tracker-comparison-card-strong">{{median.highest.[0].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.and"}} <span class="roll-tracker-comparison-card-strong">{{median.highest.[1].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{median.highest.[0].value}}.
                {{/if}}
                {{#if (isThreePlus median.highest.length)}}
                    {{#each median.highest}}
                        <span class="roll-tracker-comparison-card-strong">{{this.name}}</span>
                        {{~#if (isSecondLast @index ../median.highest.length)~}}
                            , {{localize "ROLL-TRACKER.comparison-card.misc.and"}}
                        {{~else~}}
                            {{#if (isLast @index ../median.highest.length)}}
                                {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{this.value}}.
                            {{~else~}}
                                ,
                            {{/if}}
                        {{/if}}
                    {{/each}}
                {{/if}}
            </li>
            <li class="roll-tracker-comparison-card-li">
                <span class="roll-tracker-comparison-card-badstat">{{localize "ROLL-TRACKER.comparison-card.descriptors.lowest"}}: </span>
                {{#if (isOne median.lowest.length)}}
                    <span class="roll-tracker-comparison-card-strong">{{median.lowest.[0].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{median.lowest.[0].value}}.
                {{/if}}
                {{#if (isTwo median.lowest.length)}}
                    <span class="roll-tracker-comparison-card-strong">{{median.lowest.[0].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.and"}} <span class="roll-tracker-comparison-card-strong">{{median.lowest.[1].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{median.lowest.[0].value}}.
                {{/if}}
                {{#if (isThreePlus median.lowest.length)}}
                    {{#each median.lowest}}
                        <span class="roll-tracker-comparison-card-strong">{{this.name}}</span>
                        {{~#if (isSecondLast @index ../median.lowest.length)~}}
                            , {{localize "ROLL-TRACKER.comparison-card.misc.and"}}
                        {{~else~}}
                            {{#if (isLast @index ../median.lowest.length)}}
                                {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{this.value}}.
                            {{~else~}}
                                ,
                            {{/if}}
                        {{/if}}
                    {{/each}}
                {{/if}}
            </li>
        </ul>

{{!--
* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
--}}

{{!-- MODE --}}
{{!-- The mode is computed differently from the other stats and it currently does not support displaying ties --}}
        <h3 class="roll-tracker-comparison-card-stat-heading">{{localize "ROLL-TRACKER.comparison-card.headers.mode"}}</h3>
        <ul class="roll-tracker-comparison-card-ul">
            <li class="roll-tracker-comparison-card-li">
                <span class="roll-tracker-comparison-card-italics">{{localize "ROLL-TRACKER.comparison-card.descriptors.raw"}}</span>:
                <span class="roll-tracker-comparison-card-strong">{{comparator.highest.name}}</span> {{localize "ROLL-TRACKER.comparison-card.mode.most-frequent"}} 
                {{comparator.highest.value}} {{localize "ROLL-TRACKER.comparison-card.mode.instances-of"}} {{comparator.highest.mode}}.
            </li>
            <li class="roll-tracker-comparison-card-li">
                <span class="roll-tracker-comparison-card-italics">{{localize "ROLL-TRACKER.comparison-card.descriptors.percentage"}}</span>:
                <span class="roll-tracker-comparison-card-strong">{{comparator.highestPercentage.name}}</span> {{localize "ROLL-TRACKER.comparison-card.mode.most-frequent"}} 
                {{comparator.highestPercentage.percentage}}% ({{comparator.highestPercentage.value}}) {{localize "ROLL-TRACKER.comparison-card.mode.of-their-rolls"}} {{comparator.highestPercentage.mode}}.
            </li>
        </ul>

{{!--
* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
--}}

{{!-- NAT1s --}}
{{!-- Nat1s and Nat20s are shown in both their raw number and percentage formats, for the most and fewest of each category --}}
        <h3 class="roll-tracker-comparison-card-stat-heading">{{localize "ROLL-TRACKER.comparison-card.headers.nat1s"}}</h3>
        <ul class="roll-tracker-comparison-card-ul">
            <li class="roll-tracker-comparison-card-li">
                <span class="roll-tracker-comparison-card-badstat">{{localize "ROLL-TRACKER.comparison-card.descriptors.most"}}: </span>
                <ul class="roll-tracker-comparison-card-ul">
                    {{!-- MOST BY NUMBERS--}}
                    <li class="roll-tracker-comparison-card-subli">
                        <span class="roll-tracker-comparison-card-italics">{{localize "ROLL-TRACKER.comparison-card.descriptors.raw"}}</span>: 
                        {{#if (isOne nat1s.highest.length)}}
                            <span class="roll-tracker-comparison-card-strong">{{nat1s.highest.[0].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{nat1s.highest.[0].value}}.
                        {{/if}}
                        {{#if (isTwo nat1s.highest.length)}}
                            <span class="roll-tracker-comparison-card-strong">{{nat1s.highest.[0].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.and"}} <span class="roll-tracker-comparison-card-strong">{{nat1s.highest.[1].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{nat1s.highest.[0].value}}.
                        {{/if}}
                        {{#if (isThreePlus nat1s.highest.length)}}
                            {{#each nat1s.highest}}
                                <span class="roll-tracker-comparison-card-strong">{{this.name}}</span>
                                {{~#if (isSecondLast @index ../nat1s.highest.length)~}}
                                    , {{localize "ROLL-TRACKER.comparison-card.misc.and"}}
                                {{~else~}}
                                    {{#if (isLast @index ../nat1s.highest.length)}}
                                        {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{this.value}}.
                                    {{~else~}}
                                        ,
                                    {{/if}}
                                {{/if}}
                            {{/each}}
                        {{/if}}
                    </li>

                    {{!-- MOST BY PERCENTAGE--}}
                    <li class="roll-tracker-comparison-card-subli">
                        <span class="roll-tracker-comparison-card-italics">{{localize "ROLL-TRACKER.comparison-card.descriptors.percentage"}}</span>: 
                        {{#if (isOne nat1sPercentage.highest.length)}}
                            <span class="roll-tracker-comparison-card-strong">{{nat1sPercentage.highest.[0].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{nat1sPercentage.highest.[0].value}}%.
                        {{/if}}
                        {{#if (isTwo nat1sPercentage.highest.length)}}
                            <span class="roll-tracker-comparison-card-strong">{{nat1sPercentage.highest.[0].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.and"}} <span class="roll-tracker-comparison-card-strong">{{nat1sPercentage.highest.[1].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{nat1sPercentage.highest.[0].value}}%.
                        {{/if}}
                        {{#if (isThreePlus nat1sPercentage.highest.length)}}
                            {{#each nat1sPercentage.highest}}
                                <span class="roll-tracker-comparison-card-strong">{{this.name}}</span>
                                {{~#if (isSecondLast @index ../nat1sPercentage.highest.length)~}}
                                    , {{localize "ROLL-TRACKER.comparison-card.misc.and"}}
                                {{~else~}}
                                    {{#if (isLast @index ../nat1sPercentage.highest.length)}}
                                        {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{this.value}}%.
                                    {{~else~}}
                                        ,
                                    {{/if}}
                                {{/if}}
                            {{/each}}
                        {{/if}}
                    </li>
                    </li>
                </ul>    
            </li>

            <li class="roll-tracker-comparison-card-li">
                <span class="roll-tracker-comparison-card-goodstat">{{localize "ROLL-TRACKER.comparison-card.descriptors.fewest"}}: </span>
                <ul class="roll-tracker-comparison-card-ul">
                    {{!-- FEWEST BY NUMBERS--}}
                    <li class="roll-tracker-comparison-card-subli">
                        <span class="roll-tracker-comparison-card-italics">{{localize "ROLL-TRACKER.comparison-card.descriptors.raw"}}</span>: 
                        {{#if (isOne nat1s.lowest.length)}}
                            <span class="roll-tracker-comparison-card-strong">{{nat1s.lowest.[0].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{nat1s.lowest.[0].value}}.
                        {{/if}}
                        {{#if (isTwo nat1s.lowest.length)}}
                            <span class="roll-tracker-comparison-card-strong">{{nat1s.lowest.[0].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.and"}} <span class="roll-tracker-comparison-card-strong">{{nat1s.lowest.[1].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{nat1s.lowest.[0].value}}.
                        {{/if}}
                        {{#if (isThreePlus nat1s.lowest.length)}}
                            {{#each nat1s.lowest}}
                                <span class="roll-tracker-comparison-card-strong">{{this.name}}</span>
                                {{~#if (isSecondLast @index ../nat1s.lowest.length)~}}
                                    , {{localize "ROLL-TRACKER.comparison-card.misc.and"}}
                                {{~else~}}
                                    {{#if (isLast @index ../nat1s.lowest.length)}}
                                        {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{this.value}}.
                                    {{~else~}}
                                        ,
                                    {{/if}}
                                {{/if}}
                            {{/each}}
                        {{/if}}
                    </li>

                    {{!-- FEWEST BY PERCENTAGE--}}
                    <li class="roll-tracker-comparison-card-subli">
                        <span class="roll-tracker-comparison-card-italics">{{localize "ROLL-TRACKER.comparison-card.descriptors.percentage"}}</span>: 
                        {{#if (isOne nat1sPercentage.lowest.length)}}
                            <span class="roll-tracker-comparison-card-strong">{{nat1sPercentage.lowest.[0].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{nat1sPercentage.lowest.[0].value}}%.
                        {{/if}}
                        {{#if (isTwo nat1sPercentage.lowest.length)}}
                            <span class="roll-tracker-comparison-card-strong">{{nat1sPercentage.lowest.[0].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.and"}} <span class="roll-tracker-comparison-card-strong">{{nat1sPercentage.lowest.[1].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{nat1sPercentage.lowest.[0].value}}%.
                        {{/if}}
                        {{#if (isThreePlus nat1sPercentage.lowest.length)}}
                            {{#each nat1sPercentage.lowest}}
                                <span class="roll-tracker-comparison-card-strong">{{this.name}}</span>
                                {{~#if (isSecondLast @index ../nat1sPercentage.lowest.length)~}}
                                    , {{localize "ROLL-TRACKER.comparison-card.misc.and"}}
                                {{~else~}}
                                    {{#if (isLast @index ../nat1sPercentage.lowest.length)}}
                                        {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{this.value}}%.
                                    {{~else~}}
                                        ,
                                    {{/if}}
                                {{/if}}
                            {{/each}}
                        {{/if}}
                    </li>
                    </li>
                </ul>    
            </li>

        </ul>

{{!--
* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
--}}

{{!-- NAT20s --}}
        <h3 class="roll-tracker-comparison-card-stat-heading">{{localize "ROLL-TRACKER.comparison-card.headers.nat20s"}}</h3>
        <ul class="roll-tracker-comparison-card-ul">
            <li class="roll-tracker-comparison-card-li">
                <span class="roll-tracker-comparison-card-goodstat">{{localize "ROLL-TRACKER.comparison-card.descriptors.most"}}: </span>
                <ul class="roll-tracker-comparison-card-ul">
                    {{!-- MOST BY NUMBERS--}}
                    <li class="roll-tracker-comparison-card-subli">
                        <span class="roll-tracker-comparison-card-italics">{{localize "ROLL-TRACKER.comparison-card.descriptors.raw"}}</span>: 
                        {{#if (isOne nat20s.highest.length)}}
                            <span class="roll-tracker-comparison-card-strong">{{nat20s.highest.[0].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{nat20s.highest.[0].value}}.
                        {{/if}}
                        {{#if (isTwo nat20s.highest.length)}}
                            <span class="roll-tracker-comparison-card-strong">{{nat20s.highest.[0].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.and"}} <span class="roll-tracker-comparison-card-strong">{{nat20s.highest.[1].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{nat20s.highest.[0].value}}.
                        {{/if}}
                        {{#if (isThreePlus nat20s.highest.length)}}
                            {{#each nat20s.highest}}
                                <span class="roll-tracker-comparison-card-strong">{{this.name}}</span>
                                {{~#if (isSecondLast @index ../nat20s.highest.length)~}}
                                    , {{localize "ROLL-TRACKER.comparison-card.misc.and"}}
                                {{~else~}}
                                    {{#if (isLast @index ../nat20s.highest.length)}}
                                        {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{this.value}}.
                                    {{~else~}}
                                        ,
                                    {{/if}}
                                {{/if}}
                            {{/each}}
                        {{/if}}
                    </li>

                    {{!-- MOST BY PERCENTAGE--}}
                    <li class="roll-tracker-comparison-card-subli">
                        <span class="roll-tracker-comparison-card-italics">{{localize "ROLL-TRACKER.comparison-card.descriptors.percentage"}}</span>: 
                        {{#if (isOne nat20sPercentage.highest.length)}}
                            <span class="roll-tracker-comparison-card-strong">{{nat20sPercentage.highest.[0].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{nat20sPercentage.highest.[0].value}}%.
                        {{/if}}
                        {{#if (isTwo nat20sPercentage.highest.length)}}
                            <span class="roll-tracker-comparison-card-strong">{{nat20sPercentage.highest.[0].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.and"}} <span class="roll-tracker-comparison-card-strong">{{nat20sPercentage.highest.[1].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{nat20sPercentage.highest.[0].value}}%.
                        {{/if}}
                        {{#if (isThreePlus nat20sPercentage.highest.length)}}
                            {{#each nat20sPercentage.highest}}
                                <span class="roll-tracker-comparison-card-strong">{{this.name}}</span>
                                {{~#if (isSecondLast @index ../nat20sPercentage.highest.length)~}}
                                    , {{localize "ROLL-TRACKER.comparison-card.misc.and"}}
                                {{~else~}}
                                    {{#if (isLast @index ../nat20sPercentage.highest.length)}}
                                        {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{this.value}}%.
                                    {{~else~}}
                                        ,
                                    {{/if}}
                                {{/if}}
                            {{/each}}
                        {{/if}}
                    </li>
                    </li>
                </ul>    
            </li>

            <li class="roll-tracker-comparison-card-li">
                <span class="roll-tracker-comparison-card-badstat">{{localize "ROLL-TRACKER.comparison-card.descriptors.fewest"}}: </span>
                <ul class="roll-tracker-comparison-card-ul">
                    {{!-- FEWEST BY NUMBERS--}}
                    <li class="roll-tracker-comparison-card-subli">
                        <span class="roll-tracker-comparison-card-italics">{{localize "ROLL-TRACKER.comparison-card.descriptors.raw"}}</span>: 
                        {{#if (isOne nat20s.lowest.length)}}
                            <span class="roll-tracker-comparison-card-strong">{{nat20s.lowest.[0].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{nat20s.lowest.[0].value}}.
                        {{/if}}
                        {{#if (isTwo nat20s.lowest.length)}}
                            <span class="roll-tracker-comparison-card-strong">{{nat20s.lowest.[0].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.and"}} <span class="roll-tracker-comparison-card-strong">{{nat20s.lowest.[1].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{nat20s.lowest.[0].value}}.
                        {{/if}}
                        {{#if (isThreePlus nat20s.lowest.length)}}
                            {{#each nat20s.lowest}}
                                <span class="roll-tracker-comparison-card-strong">{{this.name}}</span>
                                {{~#if (isSecondLast @index ../nat20s.lowest.length)~}}
                                    , {{localize "ROLL-TRACKER.comparison-card.misc.and"}}
                                {{~else~}}
                                    {{#if (isLast @index ../nat20s.lowest.length)}}
                                        {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{this.value}}.
                                    {{~else~}}
                                        ,
                                    {{/if}}
                                {{/if}}
                            {{/each}}
                        {{/if}}
                    </li>

                    {{!-- FEWEST BY NUMBERS--}}
                    <li class="roll-tracker-comparison-card-subli">
                        <span class="roll-tracker-comparison-card-italics">{{localize "ROLL-TRACKER.comparison-card.descriptors.percentage"}}</span>: 
                        {{#if (isOne nat20sPercentage.lowest.length)}}
                            <span class="roll-tracker-comparison-card-strong">{{nat20sPercentage.lowest.[0].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{nat20sPercentage.lowest.[0].value}}%.
                        {{/if}}
                        {{#if (isTwo nat20sPercentage.lowest.length)}}
                            <span class="roll-tracker-comparison-card-strong">{{nat20sPercentage.lowest.[0].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.and"}} <span class="roll-tracker-comparison-card-strong">{{nat20sPercentage.lowest.[1].name}}</span> {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{nat20sPercentage.lowest.[0].value}}%.
                        {{/if}}
                        {{#if (isThreePlus nat20sPercentage.lowest.length)}}
                            {{#each nat20sPercentage.lowest}}
                                <span class="roll-tracker-comparison-card-strong">{{this.name}}</span>
                                {{~#if (isSecondLast @index ../nat20sPercentage.lowest.length)~}}
                                    , {{localize "ROLL-TRACKER.comparison-card.misc.and"}}
                                {{~else~}}
                                    {{#if (isLast @index ../nat20sPercentage.lowest.length)}}
                                        {{localize "ROLL-TRACKER.comparison-card.misc.with"}} {{this.value}}%.
                                    {{~else~}}
                                        ,
                                    {{/if}}
                                {{/if}}
                            {{/each}}
                        {{/if}}
                    </li>
                    </li>
                </ul>    
            </li>

        </ul>

    </div>
</form>